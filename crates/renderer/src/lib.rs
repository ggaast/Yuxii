
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        /*
            let mut scene = Scene::new();

            let mut asset_server = AssetServer::new();

            asset_server.load(["my_model.gltf", "another.glb"]);
            
            scene <<= Point{
                ..Default::default()
            };

            scene <<= asset_server.handle("my_model.gltf");

            scene <<= Camera{

            };

            scene <<= Pbr{
                mesh: asset_server
            };
        
        */
    }
}
